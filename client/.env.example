# REST API base (must include /api)
VITE_API_BASE_URL=http://localhost:5000/api

# Socket.IO server origin (no /api). Defaults to API host without /api when omitted.
VITE_SOCKET_URL=http://localhost:5000
